import React from 'react'

const withCurrency = (BaseComponent) => {
    class Currency extends Component {
        render() {
            return(
                <div></div>
            )
        }
        }
    }
